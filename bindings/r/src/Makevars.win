# On Windows the C ABI DLL is linked directly (mingw resolves the import stubs
# from wickra_timemachine.dll); the loader finds it via PATH at run time.
PKG_CPPFLAGS = -I$(WKTIMEMACHINE_INC)
PKG_LIBS = -L$(WKTIMEMACHINE_LIB) -l:wickra_timemachine.dll
